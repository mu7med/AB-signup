# AB-signup

AtomicMail account creation bot with multi-step form filling, manual CAPTCHA handling, seed phrase capture, and SQLite database storage.

## Quick Start

```bash
pip install -r requirements.txt
playwright install chromium
cp .env.example .env
python atomicmail_bot.py
```

See full documentation in the repository files.
